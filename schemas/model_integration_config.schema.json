{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://robotactile.invalid/schemas/model_integration_config.schema.json",
  "title": "RoboTactile Model Integration Config",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "artifact_manifest",
    "device",
    "integration_id",
    "schema_version",
    "transport"
  ],
  "properties": {
    "artifact_manifest": {"type": "string", "minLength": 1},
    "device": {"type": "string", "minLength": 1},
    "integration_id": {"enum": ["act", "dream_tac", "ftp1_policy", "n0_twam", "n0_vtla"]},
    "schema_version": {"const": "robotactile-model-integration-config-v1"},
    "transport": {"enum": ["in_process", "official_http", "official_websocket", "official_zmq"]}
  }
}
